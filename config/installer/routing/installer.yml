an602_installer_index:
    path: /
    defaults:
        _controller: an602.installer.controller.welcome:handle
        mode: 'intro'

an602_installer_license:
    path: /license
    defaults:
        _controller: an602.installer.controller.welcome:handle
        mode: 'license'

an602_installer_support:
    path: /support
    defaults:
        _controller: an602.installer.controller.welcome:handle
        mode: 'support'

an602_installer_install:
    path: /install
    defaults:
        _controller: an602.installer.controller.install:handle

an602_installer_update:
    path: /update
    defaults:
        _controller: an602.installer.controller.update:handle

an602_installer_update_file_download:
    path: /download/updated
    defaults:
        _controller: an602.installer.controller.file_downloader:update_archive

an602_installer_update_conflict_download:
    path: /download/conflict
    defaults:
        _controller: an602.installer.controller.file_downloader:conflict_archive

an602_convert_intro:
    path: /convert/{start_new}
    defaults:
        _controller: an602.installer.controller.convert:intro
        start_new: 0

an602_convert_settings:
    path: /convert/settings/{converter}
    defaults:
        _controller: an602.installer.controller.convert:settings
    requirements:
        converter: "[a-zA-Z0-9_]+"

an602_convert_convert:
    path: /convert/in_progress/{converter}
    defaults:
        _controller: an602.installer.controller.convert:convert
    requirements:
        converter: "[a-zA-Z0-9_]+"

an602_convert_finish:
    path: /convert/finished
    defaults:
        _controller: an602.installer.controller.convert:finish

an602_installer_status:
    path: /installer/status
    defaults:
        _controller: an602.installer.controller.status:status
