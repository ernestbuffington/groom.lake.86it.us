services:
    an602.installer.controller.welcome:
        class: an602\install\controller\installer_index
        arguments:
            - '@an602.installer.controller.helper'
            - '@language'
            - '@template'
            - '%core.root_path%'

    an602.installer.controller.helper:
        class: an602\install\controller\helper
        arguments:
            - '@installer.helper.config'
            - '@language'
            - '@language.helper.language_file'
            - '@installer.navigation.provider'
            - '@template'
            - '@path_helper'
            - '@request'
            - '@symfony_request'
            - '@router'
            - '%core.root_path%'

    an602.installer.controller.install:
        class: an602\install\controller\install
        arguments:
            - '@an602.installer.controller.helper'
            - '@installer.helper.iohandler_factory'
            - '@installer.navigation.provider'
            - '@language'
            - '@template'
            - '@request'
            - '@installer.installer.install'
            - '@installer.helper.install_helper'

    an602.installer.controller.update:
        class: an602\install\controller\update
        arguments:
            - '@an602.installer.controller.helper'
            - '@installer.installer.update'
            - '@installer.helper.install_helper'
            - '@installer.helper.iohandler_factory'
            - '@language'
            - '@installer.navigation.provider'
            - '@request'
            - '@template'

    an602.installer.controller.file_downloader:
        class: an602\install\controller\archive_download
        arguments:
            - '@installer.helper.config'

    an602.installer.controller.convert:
        class: an602\convert\controller\convertor
        arguments:
            - '@cache.driver'
            - '@installer.helper.container_factory'
            - '@installer.helper.database'
            - '@an602.installer.controller.helper'
            - '@installer.helper.install_helper'
            - '@installer.helper.iohandler_factory'
            - '@language'
            - '@installer.navigation.provider'
            - '@request'
            - '@template'
            - '%core.root_path%'
            - '%core.php_ext%'

    an602.installer.controller.status:
        class: an602\install\controller\timeout_check
        arguments:
            - '@an602.installer.controller.helper'
            - '%core.root_path%'
