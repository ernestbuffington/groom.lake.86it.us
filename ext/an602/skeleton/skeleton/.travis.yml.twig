language: php
{% if skeleton_version_compare(REQUIREMENTS.an602_version_max, "3.2", "<") %} {# for AN602 3.1.x #}
dist: trusty

matrix:
  include:
    - php: 5.3.3
      env: DB=mysqli
    - php: 5.3
      env: DB=mysqli # MyISAM
    - php: 5.4
      env: DB=mysqli
    - php: 5.4
      env: DB=mysql
    - php: 5.4
      env: DB=mariadb
    - php: 5.4
      env: DB=postgres
    - php: 5.4
      env: DB=sqlite3
    - php: 5.5
      env: DB=mysqli
    - php: 5.6
      env: DB=mysqli
    - php: hhvm
      env: DB=mysqli
  allow_failures:
    - php: hhvm
  fast_finish: true

env:
  global:
    - EXTNAME="{{ EXTENSION.vendor_name }}/{{ EXTENSION.extension_name }}"  # CHANGE name of the extension HERE
    - SNIFF="1"            # Should we run code sniffer on your code?
    - IMAGE_ICC="1"        # Should we run icc profile sniffer on your images?
    - EPV="1"              # Should we run EPV (Extension Pre Validator) on your code?
    - AN602_BRANCH="3.1.x"

branches:
  only:
    - master
    - /^\d+(\.\d+)?\.x$/

install:
  - travis/prepare-an602.sh $AN602_BRANCH
  - cd ../../AN6023
  - travis/prepare-extension.sh $EXTNAME $AN602_BRANCH
  - travis/setup-an602.sh $DB $TRAVIS_PHP_VERSION
  - sh -c "if [ '$EPV' != '0' -a '$TRAVIS_PHP_VERSION' = '5.5' -a '$DB' = 'mysqli' ]; then cd AN602; composer remove sami/sami --dev --no-interaction; composer require an602/epv:dev-master --dev --no-interaction --ignore-platform-reqs; cd ../; fi"

before_script:
  - travis/setup-database.sh $DB $TRAVIS_PHP_VERSION

script:
  - sh -c "if [ '$SNIFF' != '0' ]; then travis/ext-sniff.sh $DB $TRAVIS_PHP_VERSION $EXTNAME; fi"
  - sh -c "if [ '$IMAGE_ICC' != '0' ]; then travis/check-image-icc-profiles.sh $DB $TRAVIS_PHP_VERSION; fi"
  - AN602/vendor/bin/phpunit --configuration AN602/ext/$EXTNAME/travis/phpunit-$DB-travis.xml --bootstrap ./tests/bootstrap.php
  - sh -c "if [ '$EPV' != '0' -a '$TRAVIS_PHP_VERSION' = '5.5' -a '$DB' = 'mysqli' ]; then AN602/vendor/bin/EPV.php run --dir='AN602/ext/$EXTNAME/'; fi"
{% elseif skeleton_version_compare(REQUIREMENTS.an602_version_max, "3.3", "<") %}{# for AN602 > 3.2.x #}
dist: trusty

matrix:
  include:
    - php: 5.5
      env: DB=none;NOTESTS=1
    - php: 5.4
      env: DB=mysqli # MyISAM
    - php: 5.4
      env: DB=mysql
    - php: 5.4
      env: DB=mariadb
    - php: 5.4
      env: DB=postgres
    - php: 5.4
      env: DB=sqlite3
    - php: 5.5
      env: DB=mysqli
    - php: 5.6
      env: DB=mysqli
    - php: 7.0
      env: DB=mysqli
    - php: 7.1
      env: DB=mysqli
    - php: 7.2
      env: DB=mysqli
    - php: nightly
      env: DB=mysqli
  allow_failures:
    - php: nightly
  fast_finish: true

env:
  global:
    - EXTNAME="{{ EXTENSION.vendor_name }}/{{ EXTENSION.extension_name }}"  # CHANGE name of the extension HERE
    - SNIFF="1"            # Should we run code sniffer on your code?
    - IMAGE_ICC="1"        # Should we run icc profile sniffer on your images?
    - EPV="1"              # Should we run EPV (Extension Pre Validator) on your code?
    - AN602_BRANCH="3.2.x"

branches:
  only:
    - master
    - /^\d+(\.\d+)?\.x$/

install:
  - travis/prepare-an602.sh $AN602_BRANCH
  - cd ../../AN6023
  - travis/prepare-extension.sh $EXTNAME $AN602_BRANCH
  - travis/setup-an602.sh $DB $TRAVIS_PHP_VERSION $NOTESTS
  - sh -c "if [ '$EPV' = '1' -a '$NOTESTS' = '1' ]; then cd AN602; composer remove sami/sami --dev --no-interaction; composer require an602/epv:dev-master --dev --no-interaction --ignore-platform-reqs; cd ../; fi"

before_script:
  - travis/setup-database.sh $DB $TRAVIS_PHP_VERSION $NOTESTS

script:
  - sh -c "if [ '$SNIFF' != '0' ]; then travis/ext-sniff.sh $DB $TRAVIS_PHP_VERSION $EXTNAME $NOTESTS; fi"
  - sh -c "if [ '$IMAGE_ICC' != '0' ]; then travis/check-image-icc-profiles.sh $DB $TRAVIS_PHP_VERSION $NOTESTS; fi"
  - sh -c "if [ '$EPV' != '0' -a '$NOTESTS' = '1' ]; then AN602/vendor/bin/EPV.php run --dir='AN602/ext/$EXTNAME/'; fi"
  - sh -c "if [ '$NOTESTS' != '1' ]; then AN602/vendor/bin/phpunit --configuration AN602/ext/$EXTNAME/travis/phpunit-$DB-travis.xml --bootstrap ./tests/bootstrap.php; fi"
{% else %}
dist: xenial

matrix:
  include:
    - php: 7.1
      env: DB=none;NOTESTS=1
    - php: 7.1
      env: DB=mariadb
    - php: 7.1
      env: DB=postgres
    - php: 7.1
      env: DB=sqlite3
    - php: 7.1
      env: DB=mysqli # MyISAM
    - php: 7.2
      env: DB=mysqli
    - php: 7.3
      env: DB=mysqli
    - php: 7.4
      env: DB=mysqli
    - php: nightly
      env: DB=mysqli
  allow_failures:
    - php: nightly
  fast_finish: true

env:
  global:
    - EXTNAME="{{ EXTENSION.vendor_name }}/{{ EXTENSION.extension_name }}"  # CHANGE name of the extension HERE
    - SNIFF="1"            # Should we run code sniffer on your code?
    - IMAGE_ICC="1"        # Should we run icc profile sniffer on your images?
    - EPV="1"              # Should we run EPV (Extension Pre Validator) on your code?
    - AN602_BRANCH="3.3.x"

branches:
  only:
    - master
    - /^\d+(\.\d+)?\.x$/

services:
  - postgresql
  - mysql

install:
  - travis/prepare-an602.sh $AN602_BRANCH
  - cd ../../AN6023
  - travis/prepare-extension.sh $EXTNAME $AN602_BRANCH
  - travis/setup-an602.sh $DB $TRAVIS_PHP_VERSION $NOTESTS
  - sh -c "if [ '$EPV' = '1' -a '$NOTESTS' = '1' ]; then cd AN602; composer require an602/epv:dev-master --dev --no-interaction --ignore-platform-reqs; cd ../; fi"

before_script:
  - travis/setup-database.sh $DB $TRAVIS_PHP_VERSION $NOTESTS

script:
  - sh -c "if [ '$SNIFF' != '0' ]; then travis/ext-sniff.sh $DB $TRAVIS_PHP_VERSION $EXTNAME $NOTESTS; fi"
  - sh -c "if [ '$IMAGE_ICC' != '0' ]; then travis/check-image-icc-profiles.sh $DB $TRAVIS_PHP_VERSION $NOTESTS; fi"
  - sh -c "if [ '$EPV' != '0' -a '$NOTESTS' = '1' ]; then AN602/vendor/bin/EPV.php run --dir='AN602/ext/$EXTNAME/'; fi"
  - sh -c "if [ '$NOTESTS' != '1' ]; then AN602/vendor/bin/phpunit --configuration AN602/ext/$EXTNAME/travis/phpunit-$DB-travis.xml --bootstrap ./tests/bootstrap.php; fi"
{% endif %}
